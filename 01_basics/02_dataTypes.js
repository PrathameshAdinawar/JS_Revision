"use strict"; // treat all JS code as new version

//alert("Pratham")// give error as we are not using browser its nodejs
// maintain code redability "high priority"

let name = "Prathamesh" //String 
let phno = 999999 // int (2^52) or Bigint way bigger for finance 
let islogin = true //boolean true or false
// null -> stand alone value and is a special type (for example temperature as 0 is a value then we use this null)
// undefined -> variable declared but not defined value 
// Symbols -> unique 
//Object -> big topic will see as we go 
// typeof -> to know which datatype is variable 
console.log(typeof name); // String 
console.log(typeof null); // Object 